t=int(input())
for i in range(t):
    n,k,d=map(int,input().split())
    x=map(int,input().split())
    add=sum(x)
    value=add//k
    if add<k:
        print(0)
    elif add==k:
        print(1)
    else:
        if value<d:
            print(value)
        else:
            print(d)
